import { BaseLayout } from '@/layout/base-layout';

const Account: React.FC = (): JSX.Element => {
  return (
    <BaseLayout>
      <main>
        <h1>Account page</h1>
      </main>
    </BaseLayout>
  );
};

export default Account;
